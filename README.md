# Dream Surfer

Browser prototype: Vite + React + TypeScript + Three.js. See [prd.md](./prd.md) for vision, scope, and module plan.

## Local

```bash
npm install
npm run dev
```

## Vercel

Connect this GitHub repository in Vercel, choose **Vite**, defaults: build `npm run build`, output `dist`.
